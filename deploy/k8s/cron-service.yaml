apiVersion: v1
kind: Service
metadata:
  name: owlclaw-cron
  labels:
    app: owlclaw-cron
spec:
  selector:
    app: owlclaw-cron
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
