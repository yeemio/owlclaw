apiVersion: v1
kind: Service
metadata:
  name: owlhub-api
  labels:
    app: owlhub-api
spec:
  type: ClusterIP
  selector:
    app: owlhub-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
