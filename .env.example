# Copy this file to .env and edit values. .env is gitignored.
# Format: KEY=value (no quotes unless needed).

# === Required: local database defaults (minimal/test/dev) ===
# Required. Default local DSN for app/runtime.
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/owlclaw
# Required. OwlClaw DB URL used by CLI/runtime.
OWLCLAW_DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/owlclaw
# Optional. Admin URL used by `owlclaw db init`.
OWLCLAW_ADMIN_URL=postgresql://postgres:postgres@localhost:5432/postgres

# === Hatchet (optional, needed for durable scheduler/integration tests) ===
# Optional. Client token from Hatchet UI.
HATCHET_API_TOKEN=
# Optional. gRPC/HTTP endpoint exposed by local compose.
HATCHET_SERVER_URL=http://localhost:17077
# Optional. gRPC TLS strategy: none|insecure|tls.
HATCHET_GRPC_TLS_STRATEGY=none
# Optional. Explicit host:port override for SDK.
HATCHET_GRPC_HOST_PORT=
# Optional. E2E sleep tuning for integration tests.
HATCHET_E2E_SLEEP_SECONDS=8
# Optional. Run expensive Hatchet perf tests when set to 1.
HATCHET_RUN_PERF_1000=0

# === Langfuse (optional tracing) ===
# Optional. Public key from Langfuse project settings.
LANGFUSE_PUBLIC_KEY=
# Optional. Secret key from Langfuse project settings.
LANGFUSE_SECRET_KEY=
# Optional. Local default when using docker-compose.dev.yml.
LANGFUSE_HOST=http://localhost:3000

# === Redis (optional, queue idempotency) ===
# Optional. Redis URL for queue trigger idempotency store.
REDIS_URL=redis://localhost:6379/0

# === Queue trigger: Kafka (optional) ===
# Optional. Broker endpoints.
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
# Optional. Main topic.
KAFKA_TOPIC=agent-events
# Optional. Consumer group.
KAFKA_CONSUMER_GROUP=owlclaw-agents
# Optional. Dead-letter topic.
KAFKA_DLQ_TOPIC=agent-events.dlq
# Optional. PLAINTEXT|SASL_SSL|SSL.
KAFKA_SECURITY_PROTOCOL=PLAINTEXT
# Optional. PLAIN|SCRAM-SHA-256|SCRAM-SHA-512.
KAFKA_SASL_MECHANISM=
# Optional. SASL user.
KAFKA_USERNAME=
# Optional. SASL password.
KAFKA_PASSWORD=

# === Queue trigger: RabbitMQ (optional) ===
# Optional. RabbitMQ URL.
RABBITMQ_URL=amqp://guest:guest@localhost:5672/
# Optional. Queue name.
RABBITMQ_QUEUE=agent-events
# Optional. Exchange name.
RABBITMQ_EXCHANGE=
# Optional. Routing key.
RABBITMQ_ROUTING_KEY=
# Optional. Dead-letter queue.
RABBITMQ_DLQ_QUEUE=agent-events.dlq

# === Queue trigger: AWS SQS (optional) ===
# Optional. AWS region.
AWS_REGION=us-east-1
# Optional. Queue URL.
SQS_QUEUE_URL=
# Optional. Dead-letter queue URL.
SQS_DLQ_URL=
# Optional. AWS credentials.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=

# === Runtime optional overrides ===
# Optional. Runtime service URL (if running API process).
AGENT_RUNTIME_URL=http://localhost:8000
# Optional. Webhook trigger defaults.
OWLCLAW_WEBHOOK_TIMEOUT_SECONDS=
OWLCLAW_WEBHOOK_MAX_RETRIES=
OWLCLAW_WEBHOOK_LOG_LEVEL=

# Optional. Config manager path override.
OWLCLAW_CONFIG=
# Optional. LangChain adapter toggles.
OWLCLAW_LANGCHAIN_ENABLED=
OWLCLAW_LANGCHAIN_TIMEOUT=
OWLCLAW_LANGCHAIN_TRACING_ENABLED=

# Optional. Trigger/governance overrides (examples).
OWLCLAW_TRIGGERS__CRON__ENABLED=
OWLCLAW_TRIGGERS__CRON__MAX_CONCURRENT=
OWLCLAW_TRIGGERS__GOVERNANCE__MAX_DAILY_RUNS=
OWLCLAW_TRIGGERS__RETRY__MAX_RETRIES=
OWLCLAW_INTEGRATIONS__HATCHET__SERVER_URL=
OWLCLAW_INTEGRATIONS__HATCHET__API_TOKEN=

# === Memory optional overrides ===
OWLCLAW_QDRANT_URL=
OWLCLAW_QDRANT_COLLECTION=
MEMORY_PERF_DATASET_SIZE=
MEMORY_PERF_QUERY_RUNS=

# === LLM provider keys (optional, integration tests only) ===
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
SILICONFLOW_API_KEY=

# === OwlHub optional local API/runtime ===
OWLHUB_AUTH_SECRET=
OWLHUB_CSRF_TOKEN=
OWLHUB_LOG_LEVEL=INFO
OWLHUB_INDEX_PATH=./index.json
OWLHUB_REVIEW_DIR=./.owlhub/reviews
OWLHUB_AUDIT_LOG=./.owlhub/audit.log.jsonl
OWLHUB_STATISTICS_DB=./.owlhub/skill_statistics.json
OWLHUB_BLACKLIST_DB=./.owlhub/blacklist.json
