# MCP Contract Tests Baseline

This folder stores MCP contract fixtures and regression tests.

Initial baseline scope:

- initialize/tools/resources path contracts
- error semantics consistency with API error model
- replay fixtures for protocol drift detection

