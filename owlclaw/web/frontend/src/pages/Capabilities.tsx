import { PageShell } from "@/pages/PageShell";

export function CapabilitiesPage() {
  return (
    <PageShell
      title="Capabilities"
      description="Handlers, skills, and bindings viewer is staged for Task 6."
    />
  );
}
