import { PageShell } from "@/pages/PageShell";

export function LedgerPage() {
  return (
    <PageShell
      title="Ledger"
      description="Audit timeline, filters, and detail panel are planned in Task 4."
    />
  );
}
